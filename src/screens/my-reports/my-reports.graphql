query MyReports($projectId: ID!) {
  report {
    myReports: byProjectId(id: $projectId) {
      id

      created_at
      project {
        name
      }
      reportUnits {
        executed
      }
    }
  }
}
