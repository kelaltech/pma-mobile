# query ReportAddProject($projectId: String) {
#   report {
#     # TODO: continue here...
#   }
# }

mutation ReportAdd($input: ReportCreateInput!) {
  report {
    createReport(input: $input) {
      id
      files {
        id
        url
      }
      reportUnits {
        id
        executed
        planned
      }
      photos {
        id
        url
      }
    }
  }
}
query ReportGet($projectId: ID!) {
  project {
    getProject(id: $projectId) {
      id
      name
      sections {
        name
        sectionItems {
          id
          name
          units {
            id
            name
            quantity
            rate
            toDate
            unit
          }
        }
      }
    }
  }
}
